{
  "name": "tmux-window-manager-docs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "astro": "astro",
    "generate-types": "wrangler types",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.1.4",
    "@astrojs/starlight": "^0.41.4",
    "astro": "^7.0.2",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  }
}
